{
  "name": "reflective-storage-ui",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "description": "反思存储的本机面板：Vite + Preact，构建产物提交进仓库，运行时不依赖 node_modules",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "e2e": "node test/e2e.mjs",
    "test": "npm run typecheck && npm run build && node test/e2e.mjs"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "preact": "^10.26.4",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
