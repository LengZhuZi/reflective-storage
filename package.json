{
  "name": "reflective-storage",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "for f in tests/*.ts; do node \"$f\" || exit 1; done"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {
    "@huggingface/transformers": "^4.3.0",
    "sqlite-vec": "^0.1.9",
    "typebox": "^1.3.34"
  }
}
